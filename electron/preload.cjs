'use strict';
// Intentionally exposes no privileged APIs to the renderer.
// The simulation sandbox needs nothing from Node; contextIsolation stays on.
// Reserved for future opt-in IPC (e.g. save/load replay files via dialog).
